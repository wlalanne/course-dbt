version: 2

macros:
  - name: event_type_counter
    description: Counts the amount of event types in an aggregate
    arguments:
      - name: event_type
        type: string
        description: The event type string